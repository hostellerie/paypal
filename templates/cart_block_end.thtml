{# begin {templatelocation} #}

      <tr class="paypal-cart-summary__footer">
        <td id="jcart-footer" colspan="4">
          <div class="paypal-cart-summary__total">
            <span id="jcart-subtotal">{subtotal}</span>
            {checkout}
          </div>

{# end {templatelocation} #}